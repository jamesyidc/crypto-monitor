# 🎯 一步一步修复 - 图文并茂

## 🚀 最简单的方法：Cloudflare Dashboard

### 第 1 步：登录 Cloudflare

打开浏览器，访问：
```
https://dash.cloudflare.com
```

输入你的 Cloudflare 账号密码登录。

---

### 第 2 步：找到 D1 数据库

按照以下路径点击：

```
首页 Dashboard
    ↓
点击左侧菜单 "Workers & Pages"
    ↓
点击顶部标签 "D1"
    ↓
看到数据库列表，找到 "crypto-trading-db"
    ↓
点击数据库名称进入
```

**视觉提示**:
- 左侧菜单是垂直的导航栏
- D1 标签在页面顶部水平排列
- 数据库列表显示为卡片或表格形式

---

### 第 3 步：打开 SQL Console

在数据库详情页面：

```
顶部有几个标签:
┌─────────────────────────────────────────┐
│ Overview | Console | Metrics | Settings │
└─────────────────────────────────────────┘
          ↑
      点击这个
```

点击 **Console** 标签。

---

### 第 4 步：执行 SQL 命令

现在你应该看到一个 SQL 输入框。

#### 执行命令 1

**复制这段代码**：
```sql
ALTER TABLE trading_strategies ADD COLUMN entry_price_type TEXT DEFAULT 'unlimited';
```

**粘贴到输入框**

**点击 "Execute" 或 "Run" 按钮**

**等待结果**：
- ✅ 成功：显示 "Query executed successfully" 或类似信息
- ❌ 失败：显示错误信息（如果是 "duplicate column"，说明已存在，继续下一步）

---

#### 执行命令 2

**清空输入框**（点击 "Clear" 或手动删除）

**复制这段代码**：
```sql
ALTER TABLE trading_strategies ADD COLUMN entry_specified_price REAL;
```

**粘贴并执行**

**等待成功提示** ✅

---

#### 执行命令 3

**清空输入框**

**复制这段代码**：
```sql
ALTER TABLE trading_strategies ADD COLUMN exit_price_type TEXT DEFAULT 'unlimited';
```

**粘贴并执行**

**等待成功提示** ✅

---

#### 执行命令 4

**清空输入框**

**复制这段代码**：
```sql
ALTER TABLE trading_strategies ADD COLUMN exit_specified_price REAL;
```

**粘贴并执行**

**等待成功提示** ✅

---

### 第 5 步：验证字段已添加

**执行验证查询**：

**复制这段代码**：
```sql
PRAGMA table_info(trading_strategies);
```

**粘贴并执行**

**查看结果**：

结果会显示一个表格，包含所有字段的信息。

**找到这 4 行**（通常在最后）：

| cid | name | type | notnull | dflt_value | pk |
|-----|------|------|---------|------------|-----|
| ... | entry_price_type | TEXT | 0 | 'unlimited' | 0 |
| ... | entry_specified_price | REAL | 0 | NULL | 0 |
| ... | exit_price_type | TEXT | 0 | 'unlimited' | 0 |
| ... | exit_specified_price | REAL | 0 | NULL | 0 |

**如果看到这 4 行，说明成功！** 🎉

---

### 第 6 步：刷新网页测试

1. **回到你的应用页面**
2. **硬刷新浏览器**：
   - Windows/Linux: `Ctrl + Shift + R`
   - Mac: `Cmd + Shift + R`
3. **打开策略库**
4. **创建新策略**
5. **填写表单**：
   - ✅ 策略名称
   - ✅ 策略类型（做多/做空）
   - ✅ 买点信号
   - ✅ **买点价格类型**（新增的）
   - ✅ 卖点信号
   - ✅ **卖点价格类型**（新增的）
6. **保存策略**
7. **应该成功，没有错误！** ✅

---

## 🎯 快速检查清单

在执行前，确认：

- [ ] 我已经登录 Cloudflare Dashboard
- [ ] 我找到了 D1 数据库部分
- [ ] 我看到了 crypto-trading-db 数据库
- [ ] 我打开了 Console 标签
- [ ] 我看到了 SQL 输入框

在执行后，确认：

- [ ] 4 条 ALTER TABLE 命令都成功执行
- [ ] PRAGMA table_info 显示了 4 个新字段
- [ ] 浏览器已硬刷新
- [ ] 创建策略成功，没有错误
- [ ] 策略卡显示价格类型信息

---

## 🔍 常见界面说明

### Cloudflare Dashboard 主页

```
┌────────────────────────────────────────────┐
│  🌟 Cloudflare                    [User] ▼ │
├──────────┬─────────────────────────────────┤
│ Home     │                                 │
│ Websites │  主要内容区域                    │
│ Analytics│                                 │
│ Workers  │                                 │
│ & Pages  │  ← 点击这里                      │
│ R2       │                                 │
│ ...      │                                 │
└──────────┴─────────────────────────────────┘
```

### D1 数据库列表

```
┌────────────────────────────────────────────┐
│  Workers & Pages                           │
├────────────────────────────────────────────┤
│  Workers | KV | R2 | D1 | Queues          │
│                    ↑                       │
│                 点击这里                    │
├────────────────────────────────────────────┤
│  Databases                                 │
│  ┌──────────────────────────────────────┐ │
│  │ 📊 crypto-trading-db                 │ │
│  │ Created: 2024-XX-XX                  │ │
│  │ Size: XX MB                          │ │
│  │ [View] ← 点击这里                     │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

### Console 界面

```
┌────────────────────────────────────────────┐
│  crypto-trading-db                         │
├────────────────────────────────────────────┤
│  Overview  Console  Metrics  Settings      │
│           ↑                                │
│        点击这里                             │
├────────────────────────────────────────────┤
│  SQL Query                                 │
│  ┌──────────────────────────────────────┐ │
│  │ ALTER TABLE trading_strategies ...   │ │
│  │                                      │ │
│  │                                      │ │
│  │                                      │ │
│  └──────────────────────────────────────┘ │
│  [Execute]  [Clear]                        │
│                                            │
│  Results                                   │
│  ┌──────────────────────────────────────┐ │
│  │ ✅ Query executed successfully       │ │
│  │ Rows affected: 0                     │ │
│  └──────────────────────────────────────┘ │
└────────────────────────────────────────────┘
```

---

## 📱 移动端提示

如果你在移动设备上操作：

1. 建议使用桌面模式浏览器
2. 或者等到有电脑时再操作
3. SQL 输入框在手机上可能较小，注意仔细核对代码

---

## ⚠️ 注意事项

### 执行 SQL 时注意：

1. **一次只执行一条命令**
   - 不要把 4 条命令一起粘贴
   - 逐条执行，确认每条都成功

2. **检查代码完整性**
   - 确保末尾有分号 `;`
   - 确保没有多余的空格或换行

3. **等待执行完成**
   - 不要在命令执行中刷新页面
   - 等待成功或失败提示

4. **记录错误信息**
   - 如果有错误，复制完整的错误信息
   - 方便后续排查

---

## 🎓 SQL 命令解释

如果你想了解这些命令的含义：

### ALTER TABLE
```sql
ALTER TABLE trading_strategies
```
意思：修改 trading_strategies 表的结构

### ADD COLUMN
```sql
ADD COLUMN entry_price_type TEXT DEFAULT 'unlimited'
```
意思：
- 添加一个新字段
- 字段名：entry_price_type
- 数据类型：TEXT（文本）
- 默认值：'unlimited'（无限制）

### 为什么需要 4 个字段？

1. **entry_price_type**: 买点价格类型（开盘价、收盘价等）
2. **entry_specified_price**: 指定的买入价格（当选择"指定价格"时使用）
3. **exit_price_type**: 卖点价格类型
4. **exit_specified_price**: 指定的卖出价格

这样用户就可以精确控制在什么价格买入和卖出。

---

## 🎉 完成后的效果

### 创建策略表单

```
┌─────────────────────────────────────────┐
│ 3️⃣ 选择买点信号（入场点）               │
├─────────────────────────────────────────┤
│ 买点信号类型: [支撑买入（买点）▼]       │
│ 买点关键词: [低吸____________]          │
│ 买点价格类型: [开盘价▼] ← 新增的！      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ 4️⃣ 选择卖点信号（出场点）               │
├─────────────────────────────────────────┤
│ 卖点 1: [高抛（卖点）▼]                 │
│ 卖点关键词: [高抛____________]          │
│ 卖点价格类型: [收盘价▼] ← 新增的！      │
└─────────────────────────────────────────┘
```

### 策略卡显示

```
┌─────────────────────────────────────────┐
│ 📊 我的做多策略                         │
├─────────────────────────────────────────┤
│ ┌───────────┬───────────┬───────────┐   │
│ │ 买点      │ 卖点      │ 仓位管理  │   │
│ ├───────────┼───────────┼───────────┤   │
│ │ 支撑买入  │ 高抛      │ 分1次买入 │   │
│ │ 💲开盘价  │ 💲收盘价  │ 持仓5K线  │   │
│ │           │           │           │   │
│ │  ↑新增    │   ↑新增   │           │   │
│ └───────────┴───────────┴───────────┘   │
└─────────────────────────────────────────┘
```

---

## 💪 你可以做到！

这个操作很简单：

1. ✅ 登录 Cloudflare（你肯定已经有账号）
2. ✅ 找到 D1 → Console（3 次点击）
3. ✅ 复制粘贴 4 次（每次 10 秒）
4. ✅ 验证一下（1 次查询）
5. ✅ 刷新浏览器测试

**总用时：3-5 分钟**

现在就去做吧！加油！💪🚀
