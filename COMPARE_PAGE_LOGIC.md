# 比价页面逻辑说明

## 📊 核心逻辑（正确版本）

### 工作原理
比价页面每10分钟自动刷新一次，系统会：

1. **获取当前价格**（来自CoinGecko API）
2. **与历史极值比较**
   - 如果 `当前价格 > 历史最高价` → 更新最高价为当前价格，`high_count = 0`
   - 如果 `当前价格 < 历史最低价` → 更新最低价为当前价格，`low_count = 0`
   - 否则 → `high_count += 1` 和 `low_count += 1`
3. **动态计算占比**
   - 最高占比 = `(当前价格 / 历史最高价) × 100%`
   - 最低占比 = `(当前价格 / 历史最低价) × 100%`

## 📈 数据字段说明

| 字段 | 说明 | 更新方式 |
|------|------|----------|
| 最高价格 | 历史最高价（All-Time High） | 创新高时自动更新 |
| 最高计次 | 距离上次创新高的轮次数 | 创新高时归零，否则+1 |
| 最低价格 | 历史最低价（All-Time Low） | 创新低时自动更新 |
| 最低计次 | 距离上次创新低的轮次数 | 创新低时归零，否则+1 |
| 当前价格 | 本轮刷新的实时价格 | 每轮更新 |
| 最高占比 | 当前价格占历史最高价的百分比 | 实时计算 |
| 最低占比 | 当前价格占历史最低价的百分比 | 实时计算 |

## 🔄 实际案例

### 案例1：创新高（AAVE）
```
刷新前:
  最高价格=234.33, high_count=2
  当前价格=235.66

刷新后:
  最高价格=235.66 ✅ (更新为当前价格)
  high_count=0 ✅ (归零)
  最高占比=100% ✅ (235.66/235.66×100%)
```

### 案例2：创新低（FIL）
```
刷新前:
  最低价格=1.87398, low_count=1145
  当前价格=1.64

刷新后:
  最低价格=1.64 ✅ (更新为当前价格)
  low_count=0 ✅ (归零)
  最低占比=100% ✅ (1.64/1.64×100%)
```

### 案例3：没有创新高/低（BTC）
```
刷新前:
  最高价格=111935, high_count=1726
  最低价格=40993.90625, low_count=1144
  当前价格=84820

刷新后:
  最高价格=111935 (不变)
  high_count=1727 ✅ (+1)
  最低价格=40993.90625 (不变)
  low_count=1145 ✅ (+1)
  最高占比=75.79% ✅ (84820/111935×100%)
  最低占比=206.92% ✅ (84820/40993.90625×100%)
```

## 💡 占比含义

### 最高占比
- **100%**: 正好等于历史最高价（刚刚创新高）
- **>100%**: 超过历史最高价（不可能，因为会自动更新最高价）
- **<100%**: 低于历史最高价
  - 75.79% → 距离历史高点还有 24.21% 的上涨空间
  - 63.59% → 距离历史高点还有 36.41% 的上涨空间

### 最低占比
- **100%**: 正好等于历史最低价（刚刚创新低）
- **<100%**: 低于历史最低价（不可能，因为会自动更新最低价）
- **>100%**: 高于历史最低价
  - 103.12% → 比历史最低价高出 3.12%
  - 206.92% → 比历史最低价高出 106.92%

## ⚙️ 系统组件

### 1. analysis-scheduler（定时任务）
- 每10分钟执行一次 `POST /api/analyze`
- 自动获取最新价格
- 自动更新极值数据
- 自动递增计次

### 2. /api/compare（比价API）
- 读取 `price_extremes` 表获取极值和计次
- 读取 `coin_round_details` 表获取当前价格
- **实时计算占比**（不存储）
- 返回完整数据给前端

### 3. compare.html（比价页面）
- 每30秒自动刷新数据
- 显示所有币种的极值、计次、占比
- 颜色高亮：
  - 最高占比 ≥ 100%：绿色
  - 最高占比 < 70%：黄色
  - 最低占比 ≥ 120%：绿色
  - 最低占比 < 105%：黄色

## 📋 导入的初始数据作用

您导入的29个币种数据（`update_extremes_with_ratios.sql`）：
- ✅ **提供初始的历史极值**（最高价、最低价）
- ✅ **提供初始的计次**（high_count, low_count）
- ❌ **固定占比字段不使用**（high_ratio, low_ratio存在但不使用）

原因：
- 占比必须基于**实时价格**动态计算
- 固定占比会导致数据不准确
- 系统会自动更新极值和计次

## 🔧 数据库表结构

```sql
CREATE TABLE price_extremes (
  id INTEGER PRIMARY KEY,
  symbol TEXT UNIQUE NOT NULL,
  all_time_high REAL NOT NULL,      -- 历史最高价（自动更新）
  all_time_low REAL NOT NULL,       -- 历史最低价（自动更新）
  high_count INTEGER DEFAULT 0,     -- 最高价计次（自动更新）
  low_count INTEGER DEFAULT 0,      -- 最低价计次（自动更新）
  high_ratio REAL DEFAULT 0,        -- 固定占比字段（不使用）
  low_ratio REAL DEFAULT 0,         -- 固定占比字段（不使用）
  ath_date DATETIME,                -- 创新高时间
  atl_date DATETIME,                -- 创新低时间
  last_updated DATETIME             -- 最后更新时间
);
```

## 🎯 总结

| 项目 | 说明 |
|------|------|
| 数据来源 | 初始：导入的历史数据<br>运行中：系统自动更新 |
| 极值更新 | 创新高/低时自动更新 |
| 计次更新 | 每轮+1，创新高/低时归零 |
| 占比计算 | **实时动态计算**，不存储 |
| 刷新频率 | 10分钟/轮（自动） |
| 页面刷新 | 30秒/次（前端轮询） |

---

**关键点**：比价页面的数据是**动态的**，会随着市场价格变化而自动更新。导入的数据只是提供初始参考值。
