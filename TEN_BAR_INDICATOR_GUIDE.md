# 10格高低点突破指标说明

## 📊 功能概述

在K线查询页面新增了"10格"列，用于快速识别价格突破10周期高低点的信号。

## 🎯 计算逻辑

### 定义
- **10格** = 当前K线 + 往前的9根K线（共10根K线）
- 注意：表格从新到旧排列，"往前"指的是时间上更早的K线

### 计算步骤

对于表格中的每一行（当前K线）：

1. **收集10根K线数据**：
   - 当前这一根K线
   - 往前（时间上更早）的9根K线
   - 共计10根K线

2. **找出10根K线中的极值**：
   - 最高价（maxHigh）= 这10根K线中所有最高价的最大值
   - 最低价（minLow）= 这10根K线中所有最低价的最小值

3. **判断突破**：
   - 如果 **当前K线的最高价 = 10根K线的最高价** → 显示 **+1**（绿色）
     - 说明当前K线创了10格新高
   - 如果 **当前K线的最低价 = 10根K线的最低价** → 显示 **-1**（红色）
     - 说明当前K线创了10格新低
   - 其他情况 → 显示 **0**（灰色）
     - 当前K线在10格范围内波动

### 特殊情况
- 前9行显示 **"0"**（数据不足，无法凑足10根K线）
- 从第10行开始显示准确的计算结果
- **不存在"-"的情况**：所有行都会计算并显示结果

## 🎨 显示样式

| 值 | 含义 | 颜色 | 背景色 |
|-----|------|------|--------|
| **+1** | 创10格新高 | 绿色 | 浅绿色背景 |
| **-1** | 创10格新低 | 红色 | 浅红色背景 |
| **0** | 在10格范围内 | 灰色 | 无背景 |

## 📈 使用场景

### 1. 识别向上突破
当显示 **+1** 时：
- 价格创10周期新高
- 可能是短期上升趋势的开始
- 配合其他指标判断买入时机

### 2. 识别向下突破
当显示 **-1** 时：
- 价格创10周期新低
- 可能是短期下降趋势的开始
- 配合其他指标判断卖出时机

### 3. 观察盘整状态
连续显示 **0** 时：
- 价格在10周期范围内波动
- 市场处于盘整阶段
- 等待方向性突破

## 💡 实战示例

### 示例1：向上突破

```
行数 | 时间      | 最高价 | 最低价 | 10格  | 说明
-----|-----------|--------|--------|-------|------------------
1    | 13:00     | 110.5  | 109.8  | 0     | 前9行数据不足
2    | 12:55     | 110.3  | 109.5  | 0     | 前9行数据不足
...  | ...       | ...    | ...    | 0     | ...
9    | 12:25     | 109.8  | 109.2  | 0     | 前9行数据不足
10   | 12:20     | 110.6  | 109.5  | +1    | 创10格新高！
11   | 12:15     | 110.2  | 109.8  | 0     | 在范围内
```

在第10行，当前最高价110.6等于前面9根+当前这根（共10根）K线中的最高价，说明创了10格新高，触发 **+1** 信号。

### 示例2：向下突破

```
行数 | 时间      | 最高价 | 最低价 | 10格  | 说明
-----|-----------|--------|--------|-------|------------------
15   | 11:40     | 109.5  | 108.2  | -1    | 跌破前10格最低点！
16   | 11:35     | 109.8  | 108.5  | 0     | 在范围内
17   | 11:30     | 110.0  | 108.8  | 0     | 在范围内
```

在第15行，当前最低价108.2低于前面10根K线中的最低价，触发 **-1** 信号。

## 🔧 技术细节

### 代码实现（JavaScript）

```javascript
// 🆕 计算10格高低点突破指标
let tenBarSignal = '0'; // 默认为0

if (index + 9 < klineData.length) {
  // 收集10根K线（当前 + 往时间更早的9根）
  const tenBars = [];
  for (let i = index; i <= index + 9; i++) {
    tenBars.push(klineData[i]);
  }
  
  // 找出这10根K线中的最高价和最低价
  let maxHigh = -Infinity;
  let minLow = Infinity;
  
  tenBars.forEach(bar => {
    if (bar.high && bar.high > maxHigh) maxHigh = bar.high;
    if (bar.low && bar.low < minLow) minLow = bar.low;
  });
  
  // 当前K线的最高价和最低价
  const currentHigh = k.high || 0;
  const currentLow = k.low || 0;
  
  // 判断逻辑：当前K线是否创10格新高/新低
  if (currentHigh > 0 && currentHigh === maxHigh) {
    tenBarSignal = '+1'; // 创10格新高
  } else if (currentLow > 0 && currentLow === minLow) {
    tenBarSignal = '-1'; // 创10格新低
  }
  // 否则保持默认值 '0'
}
```

### 关键点说明

1. **数据顺序**：表格从新到旧排列，所以 `index` 越大，时间越早
2. **范围计算**：当前行 + 后面9行 = 共10根K线（时间从新到旧）
3. **边界处理**：前9行无法凑足10根K线，默认显示 "0"
4. **判断条件**：使用 `===` 判断是否相等，而不是 `>`，因为当前K线包含在这10根K线中

## ⚠️ 注意事项

### 1. 数据要求
- 需要至少10根K线才能计算
- 前9行始终显示 "-"
- 确保K线数据完整性

### 2. 使用建议
- **不要单独使用**：需结合其他指标（如成交量、RSI、MACD等）
- **确认突破**：单次 +1/-1 可能是假突破，观察后续走势
- **配合时间周期**：不同时间周期（5m/15m/1h）的10格信号意义不同

### 3. 适用场景
- ✅ 短期交易：5分钟、15分钟K线
- ✅ 快速识别：价格突破关键阻力/支撑
- ✅ 辅助判断：配合其他技术指标使用
- ❌ 长期投资：不适合作为唯一依据
- ❌ 震荡市场：可能产生较多假信号

## 📊 与其他指标的配合

### 1. 配合成交量（V1/V2）
- **+1 + V1**：放量向上突破，信号更强
- **-1 + V2**：放量向下突破，信号更强

### 2. 配合RSI
- **+1 + RSI < 70**：突破且未超买，可考虑买入
- **-1 + RSI > 30**：突破且未超卖，需警惕继续下跌

### 3. 配合SAR
- **+1 + SAR在价格下方**：多头趋势确认
- **-1 + SAR在价格上方**：空头趋势确认

## 🎓 学习要点

1. **理解10格的含义**：不是固定的10个价格点，而是10根K线的高低价范围
2. **识别突破时机**：+1/-1 出现时，注意观察后续K线是否延续
3. **结合趋势判断**：在上升趋势中的 +1 更可靠，在下降趋势中的 -1 更可靠
4. **风险控制**：任何信号都不是100%准确，需设置止损

## 📚 相关资源

- 技术指标组合使用指南
- K线形态识别手册
- 交易信号确认方法

---

**更新日期**：2025-11-01  
**版本**：v1.0  
**状态**：✅ 已上线
