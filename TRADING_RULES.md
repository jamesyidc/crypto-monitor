# 交易规则系统使用指南

## 📋 功能概述

交易规则系统是一个统一的交易权限管理模块，用于控制每个币种的交易行为：

- ✅ **能否开单**：总开关，决定该币种是否允许交易
- 📈 **能否做多**：控制是否允许开多单
- 📉 **能否做空**：控制是否允许开空单

## 🎯 核心特性

### 1. 三级权限控制
- **交易总开关**：禁用后，做多和做空自动禁用
- **做多开关**：独立控制多单权限
- **做空开关**：独立控制空单权限

### 2. 灵活的交易模式
- **双向交易**：允许做多+允许做空（默认模式）
- **仅做多**：只允许开多单，禁止空单
- **仅做空**：只允许开空单，禁止多单
- **禁止交易**：完全禁止该币种的所有交易

### 3. 批量快速设置
- **重置全部**：一键恢复所有币种为默认（允许所有交易）
- **禁止所有交易**：紧急情况下快速关闭所有交易
- **仅允许做多**：批量设置所有币种为多头模式
- **仅允许做空**：批量设置所有币种为空头模式

### 4. 实时统计面板
- **币种总数**：监控的币种数量
- **允许交易**：当前允许交易的币种数
- **允许做多**：可以开多单的币种数
- **允许做空**：可以开空单的币种数
- **禁止交易**：完全禁止交易的币种数

## 📍 访问路径

### 方式1：从特征库页面
1. 访问 `/pattern.html` 特征库页面
2. 点击顶部的 **"交易规则"** 标签页
3. 进入交易规则管理界面

### 方式2：直接访问API
```bash
# 获取所有规则
GET /api/trading-rules

# 获取统计信息
GET /api/trading-rules/stats

# 更新单个币种规则
PUT /api/trading-rules/:symbol

# 批量更新规则
POST /api/trading-rules/batch
```

## 💡 使用场景

### 场景1：市场极端波动
当市场出现极端波动时，可以：
```
1. 点击"禁止所有交易"
2. 停止所有自动开单
3. 观察市场，等待稳定
```

### 场景2：单边市场
在明显的单边行情中：
```
多头市场 → 点击"仅允许做多"
空头市场 → 点击"仅允许做空"
```

### 场景3：风险币种管理
对于高风险币种：
```
1. 找到该币种行
2. 关闭"允许交易"开关
3. 该币种将不再产生交易信号
```

### 场景4：选择性交易
只交易部分币种：
```
1. 先点击"禁止所有交易"
2. 然后逐个开启想交易的币种
3. 实现白名单交易模式
```

## 🔧 操作步骤

### 单个币种修改
1. 找到目标币种行
2. 切换对应的开关
3. 在备注栏添加说明（可选）
4. 点击"保存所有更改"按钮

### 批量修改
1. 点击顶部的快速设置按钮
2. 选择合适的批量操作
3. 确认操作提示
4. 系统自动应用到所有币种

### 查看状态
- **操作列**显示当前状态：
  - ✅ 双向交易（绿色）
  - 📈 仅做多（蓝色）
  - 📉 仅做空（橙色）
  - ❌ 禁止交易（红色）

## ⚠️ 注意事项

1. **修改需保存**：所有开关切换后，必须点击"保存所有更改"才会生效
2. **交易总开关**：关闭后，做多做空自动禁用
3. **批量操作**：会覆盖所有币种的当前设置，请谨慎使用
4. **备注信息**：建议在备注中说明修改原因，便于后续追溯

## 🔌 API集成示例

### TypeScript示例
```typescript
// 获取所有交易规则
const response = await fetch('/api/trading-rules');
const data = await response.json();
console.log(data.rules);

// 更新单个币种规则
await fetch('/api/trading-rules/BTC', {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    trading_allowed: 1,
    long_allowed: 1,
    short_allowed: 0,
    notes: '仅允许做多'
  })
});

// 批量更新
await fetch('/api/trading-rules/batch', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    updates: [
      { symbol: 'BTC', trading_allowed: 0 },
      { symbol: 'ETH', trading_allowed: 0 }
    ]
  })
});

// 快速设置 - 仅允许做多
await fetch('/api/trading-rules/long-only', { method: 'POST' });
```

## 📊 数据结构

### TradingRule对象
```typescript
{
  id: number;                    // 主键ID
  symbol: string;                // 币种符号 (BTC, ETH等)
  trading_allowed: number;       // 1=允许交易, 0=禁止交易
  long_allowed: number;          // 1=允许做多, 0=禁止做多
  short_allowed: number;         // 1=允许做空, 0=禁止做空
  notes?: string;                // 备注说明
  created_at: string;            // 创建时间
  updated_at: string;            // 更新时间
}
```

## 🎓 最佳实践

1. **定期检查**：每天开盘前检查交易规则设置
2. **记录原因**：每次修改都在备注中说明原因
3. **逐步调整**：避免频繁使用批量操作，建议逐个调整
4. **测试验证**：修改后观察信号生成是否符合预期
5. **备份配置**：重要设置可以通过API导出保存

## 🚀 后续扩展

未来可能添加的功能：
- [ ] 规则模板保存与加载
- [ ] 定时规则切换（如日内/夜间不同规则）
- [ ] 规则修改历史记录
- [ ] 基于市场条件的自动规则调整
- [ ] 规则配置导入/导出功能

---

**最后更新**: 2025-10-28
