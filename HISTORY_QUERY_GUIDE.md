# 策略信号池历史查询使用指南

## 快速开始

### 第一步：访问页面
- **模拟交易**：访问 `/trading` 页面
- **实盘交易**：访问 `/live-trading` 页面

### 第二步：查看实时信号（默认模式）
页面默认显示**实时监控模式**，自动展示最近3根K线的策略触发信号。

**特点**：
- ✅ 自动刷新（每30秒）
- ✅ 自动保存到历史记录
- ✅ 实时统计（做多/做空/总数）

### 第三步：切换到历史查询模式

1. 找到"查询模式"下拉框
2. 选择 **"历史查询（按日期）"**
3. 日期选择器自动显示

## 使用历史查询

### 方式1：快捷日期按钮（推荐）

**查看今天的信号**
```
点击 "今天" 按钮
```

**查看昨天的信号**
```
点击 "昨天" 按钮
```

**查看最近7天的信号**
```
点击 "最近7天" 按钮
```

### 方式2：自定义日期范围

1. 在"开始日期"选择框中选择起始日期
2. 在"结束日期"选择框中选择结束日期
3. 点击 **"查询历史"** 按钮

**示例**：查询2025年1月1日到1月15日的所有信号
```
开始日期: 2025-01-01
结束日期: 2025-01-15
点击 "查询历史"
```

## 查询结果说明

### 统计面板
显示在页面顶部的三个统计卡片：

| 统计项 | 说明 | 颜色 |
|--------|------|------|
| 做多信号 | BUY信号总数 | 绿色 |
| 做空信号 | SELL信号总数 | 红色 |
| 总信号数 | 所有信号总计 | 蓝色 |

### 信号列表表格
详细展示每个触发信号的信息：

| 列名 | 说明 | 示例 |
|------|------|------|
| 时间 | 信号触发时间 | 10:30:45 |
| 币种 | 交易对名称 | BTC-USDT-SWAP |
| 信号 | 信号类型 | 做多 / 做空 |
| 策略 | 策略名称 | 震荡收敛策略 |
| 价格 | 触发时的价格 | $43,250.50 |
| K线 | 第几根K线 | 第1根K线 |
| 原因 | 触发原因 | 5根K线内2次震荡收敛 |
| 指标 | 相关指标 | RSI: 45.60 |

## 常见操作

### 操作1：查看今日所有信号并分析
```
1. 切换到"历史查询"模式
2. 点击"今天"按钮
3. 查看统计：今日共触发多少信号
4. 分析表格：哪些币种信号最多
```

### 操作2：对比不同日期的信号
```
1. 查询日期A的数据，记录统计结果
2. 修改日期范围到日期B
3. 点击"查询历史"
4. 对比两个日期的信号数量和分布
```

### 操作3：追溯某个币种的历史信号
```
1. 选择日期范围（如最近7天）
2. 点击"查询历史"
3. 在表格中找到目标币种
4. 查看该币种的所有触发记录
```

### 操作4：切换回实时监控
```
1. 在"查询模式"下拉框中选择"实时监控（最近3根K线）"
2. 页面自动切换回实时模式
3. 日期选择器自动隐藏
4. 恢复30秒自动刷新
```

## 数据更新机制

### 实时模式
- ✅ **自动保存**：每次扫描的信号自动保存到历史数据库
- ✅ **自动刷新**：每30秒自动更新一次数据
- ✅ **实时统计**：统计数据实时计算

### 历史模式
- ⏸️ **停止自动刷新**：避免不必要的请求
- 🔄 **手动刷新**：点击"查询历史"按钮更新数据
- 📊 **历史统计**：基于已保存的历史数据统计

## 注意事项

### ⚠️ 重要提醒

1. **首次使用**：历史数据从启用功能后开始积累
2. **数据完整性**：只有在实时模式下扫描到的信号才会被保存
3. **日期范围**：建议查询范围不超过30天，以保证响应速度
4. **数据上限**：单次查询最多返回1000条记录

### 💡 使用技巧

1. **定期查看历史**：每周查看一次历史数据，分析策略效果
2. **对比分析**：对比不同时间段的信号分布，找出规律
3. **记录异常**：发现异常多或异常少的信号时，标记日期便于后续分析
4. **配合实时使用**：实时监控和历史查询结合使用，效果更佳

## 故障处理

### 问题：查询结果为空
**可能原因**：
- 选择的日期范围内没有数据
- 数据库连接失败

**解决办法**：
1. 确认选择的日期范围是否正确
2. 尝试查询"今天"或"昨天"
3. 刷新页面重试
4. 查看浏览器控制台的错误信息

### 问题：日期选择器不显示
**解决办法**：
1. 确认已选择"历史查询"模式
2. 刷新页面
3. 清除浏览器缓存

### 问题：统计数据不准确
**解决办法**：
1. 点击"刷新"按钮重新查询
2. 检查日期范围是否正确
3. 切换回实时模式再切换回来

## 快捷键（计划中）

| 快捷键 | 功能 | 状态 |
|--------|------|------|
| Alt+T | 查看今天 | 🔜 计划中 |
| Alt+Y | 查看昨天 | 🔜 计划中 |
| Alt+W | 查看最近7天 | 🔜 计划中 |
| Alt+R | 刷新数据 | 🔜 计划中 |

## 相关文档

- [详细技术文档](./SIGNAL_POOL_HISTORY_FEATURE.md)
- [策略信号池基础功能](./SIGNAL_POOL_FEATURE.md)
- [API接口说明](./API_DOCUMENTATION.md)

## 反馈与建议

如果您有任何问题或建议，请通过以下方式联系我们：
- GitHub Issues
- 开发团队邮箱
- 项目讨论区

---

**版本**：v1.1.0  
**更新日期**：2025-01-15  
**适用页面**：模拟交易、实盘交易
