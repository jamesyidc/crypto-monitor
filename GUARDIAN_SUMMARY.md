# 🛡️ 进程守护者系统 - 功能总结

## 📋 系统概述

进程守护者(Process Guardian)是一个自动化的进程监控和管理系统，专门为加密货币监控平台设计。

## 🎯 解决的核心问题

### 问题背景
之前发现"当天涨幅"显示"-"的根本原因是：**kline-scheduler没有运行，导致K线数据未自动刷新**。

### 解决方案
创建一个中央监控系统，自动检测并修复所有后台任务的运行状态。

## 🔍 监控范围

| 序号 | 进程名称 | 功能描述 | 执行频率 | 作用 |
|------|---------|---------|---------|------|
| 1 | **kline-scheduler** | K线数据自动同步调度器 | 每5分钟 | 从OKX API同步最新K线数据，是"当天涨幅"数据的源头 |
| 2 | **analysis-scheduler** | 价格分析调度器 | 每5分钟 | 执行价格变动分析，生成交易信号 |
| 3 | **snapshot-scheduler** | 数据快照调度器 | 每10分钟 | 保存完整首页数据，用于历史回溯 |
| 4 | **consecutive-rise-scheduler** | 连续上涨统计调度器 | 每15分钟 | 统计币种连续上涨趋势 |
| 5 | **health-monitor** | 系统健康监控器 | 每4分钟 | 监控端口、数据库、API等系统组件 |

## ✨ 核心功能

### 1. 自动监控
- ⏰ 每5分钟执行一次完整检查
- 🔍 检查进程是否存在、是否运行
- 💊 执行健康检查验证功能正常

### 2. 自动恢复
- 🚀 发现进程不存在时自动启动
- 🔄 发现进程停止时自动重启
- 🏥 发现健康异常时自动重启

### 3. Web控制台
- 🌐 访问地址: `http://localhost:3001`
- 📊 实时显示所有进程状态
- 📝 查看告警日志和重启历史
- ⚡ 自动30秒刷新

### 4. 健康检查机制

#### API健康检查
- 验证API端点是否响应
- 检查返回数据是否包含必需字段
- 超时检测

#### 文件健康检查
- 验证状态文件是否及时更新
- 检查文件是否过期

## 📊 监控流程

```
┌─────────────────────────────────────┐
│  每5分钟触发检查                      │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  1. 检查PM2进程是否存在              │
└──────────────┬──────────────────────┘
               ↓
        [不存在]  [存在]
           ↓        ↓
      ┌────────┐    ↓
      │启动进程│    ↓
      └────────┘    ↓
                    ↓
┌─────────────────────────────────────┐
│  2. 检查进程运行状态                 │
└──────────────┬──────────────────────┘
               ↓
        [停止]    [运行]
           ↓        ↓
      ┌────────┐    ↓
      │重启进程│    ↓
      └────────┘    ↓
                    ↓
┌─────────────────────────────────────┐
│  3. 执行健康检查（如已配置）          │
└──────────────┬──────────────────────┘
               ↓
      [不健康]  [健康]
           ↓        ↓
      ┌────────┐    ↓
      │重启进程│    ↓
      └────────┘    ↓
                    ↓
┌─────────────────────────────────────┐
│  ✅ 检查完成，记录日志               │
└─────────────────────────────────────┘
```

## 📁 文件结构

```
/home/user/webapp/
├── process-guardian.js           # 守护者主程序
├── start-guardian.sh             # 一键启动脚本
├── PROCESS_GUARDIAN_README.md    # 完整使用文档
├── QUICK_START.md                # 快速开始指南
├── process-guardian.log          # 守护者日志文件
└── public/static/
    └── process-status.json       # 进程状态数据（供API使用）
```

## 🚀 使用方法

### 启动守护者

```bash
# 方式1: 使用启动脚本（推荐）
./start-guardian.sh

# 方式2: 直接使用PM2
pm2 start process-guardian.js --name process-guardian
```

### 访问控制台

```bash
# 本地访问
http://localhost:3001

# 公网访问
https://3001-<sandbox-id>.sandbox.novita.ai
```

### 查看日志

```bash
# 守护者日志
pm2 logs process-guardian

# 查看所有进程
pm2 list

# 查看特定进程日志
pm2 logs kline-scheduler
```

## 📈 监控效果

### 统计数据
守护者会实时统计：
- 运行时长
- 检查次数
- 重启次数
- 失败次数

### 告警系统
自动记录：
- 进程启动/重启事件
- 健康检查失败
- 系统异常

## 🔒 可靠性保障

### 1. 自我保护
- 守护者本身由PM2管理
- PM2会自动重启崩溃的守护者

### 2. 优先级管理
- 按优先级顺序检查进程
- 最重要的进程优先恢复

### 3. 防止频繁重启
- 重启后等待5秒再检查
- 避免重启风暴

## 💡 最佳实践

### 持久化运行

```bash
# 保存PM2进程列表
pm2 save

# 设置开机自启动
pm2 startup
```

### 日志管理

```bash
# 安装日志轮转
pm2 install pm2-logrotate

# 配置日志大小
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7
```

## 🎉 效果对比

### 之前
- ❌ K线调度器手动启动
- ❌ 进程崩溃无人知晓
- ❌ 需要手动检查和重启
- ❌ "当天涨幅"经常显示"-"

### 现在
- ✅ 所有进程自动监控
- ✅ 崩溃立即检测并恢复
- ✅ Web控制台实时查看状态
- ✅ "当天涨幅"稳定显示数据

## 🔮 未来扩展

可以轻松添加更多监控：
1. 添加新进程到配置
2. 配置健康检查规则
3. 设置告警通知（邮件/短信）
4. 集成监控图表

---

**创建日期**: 2025-11-02  
**版本**: 1.0.0  
**状态**: ✅ 已部署并运行
