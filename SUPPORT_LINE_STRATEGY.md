# 📊 支撑线低吸策略完整说明

> **文档目的**: 详细说明支撑线设置和低吸机会判断的完整业务逻辑  
> **创建时间**: 2025-10-29  
> **适用范围**: Crypto Monitor 系统 - 模式特征库 - 支撑线管理

---

## 📍 功能位置

**访问路径**: 首页 → 特征库 → 支撑线管理 Tab  
**URL**: `/pattern.html` (第三个Tab)

---

## 🎯 核心功能概述

支撑线低吸策略是一个**半自动化的低买高卖辅助工具**，帮助用户：
1. **设置关键支撑价位**：为优质币种设定预期低点
2. **自动监控低吸机会**：系统自动检查价格是否接近支撑线
3. **风险控制**：结合市场策略和交易规则进行综合判断

---

## 🔑 核心逻辑：什么是"低吸机会"？

### ✅ 低吸机会的5个必要条件

**要同时满足以下全部条件，才算"低吸机会"**：

| 条件编号 | 条件名称 | 具体要求 | 说明 |
|---------|---------|---------|------|
| **1** | 币种等级限制 | `level <= 2` | 只有等级1或等级2的优质币种才能设置支撑线 |
| **2** | 已设置支撑线 | `support_price > 0` | 今天必须手动设置过支撑线价格 |
| **3** | 价格接近支撑线 | `±1%以内` | 当前价格与支撑线的距离 ≤ 1% |
| **4** | 市场策略允许 | `≠ 单边主跌` | 当前市场不能处于"单边主跌"状态 |
| **5** | 交易规则允许 | `long_allowed = 1` | 该币种的交易规则允许做多 |

---

## 📐 详细判断逻辑

### 1️⃣ 币种等级限制 (level <= 2)

**目的**: 只关注高质量币种，降低风险

#### 等级定义
**文件**: `src/services/analysisService.ts` 第372-379行

```typescript
private calculateCoinLevel(lowRatio: number, highRatio: number): CoinLevel {
  if (lowRatio >= 120 && highRatio >= 90) return 1;  // 最优质
  if (lowRatio >= 120 && highRatio >= 80) return 2;  // 高质量
  if (lowRatio >= 110 && highRatio >= 90) return 3;
  if (lowRatio >= 110 && highRatio >= 80) return 4;
  if (lowRatio < 110 && highRatio >= 90) return 5;
  return 6;  // 最低质量
}
```

#### 等级计算依据
- **lowRatio**: 当前价格 ÷ 历史最低价 × 100%
- **highRatio**: 当前价格 ÷ 历史最高价 × 100%

#### 等级1和2的特点
| 等级 | lowRatio | highRatio | 特点 |
|------|----------|-----------|------|
| **等级1** | ≥ 120% | ≥ 90% | 远离历史低点，接近历史高点 |
| **等级2** | ≥ 120% | ≥ 80% | 远离历史低点，相对高位 |

**为什么只允许等级1-2？**
- 这些币种已经远离历史最低价（120%+），价格健康
- 接近历史高位，说明有上涨动能
- 质量好，回调后反弹概率大

---

### 2️⃣ 支撑线价格设置

**目的**: 人工判断预期的回调支撑位

#### 如何设置支撑线？
**路径**: 模式特征库 → 支撑线管理 → 找到币种 → 在"支撑线价格"列输入数字

**示例**（从你的截图）：
- **TAO**: 设置支撑线 = 404
- **BCH**: 设置支撑线 = 553
- **BNB**: 设置支撑线 = 1126

#### 设置建议
1. **技术分析参考**：
   - 前期低点
   - 重要均线位置（如20日均线）
   - 斐波那契回调位
   - 整数关口

2. **保守设置**：
   - 建议比当前价低 **3%-5%**
   - 不要设置太近（避免频繁触发）
   - 不要设置太远（可能永远不触发）

3. **动态调整**：
   - 支撑线每天自动清零（0点）
   - 每天根据市场情况重新设置

#### 数据存储
**表**: `support_lines`
**字段**: 
- `symbol`: 币种符号
- `support_price`: 支撑线价格
- `date`: 设置日期（北京时间）
- `notes`: 备注（可选）

**每日自动清零**：
- 每天北京时间0点，自动删除前一天的所有支撑线
- 需要每天重新设置

---

### 3️⃣ 距离计算与判断 (±1%)

**目的**: 判断当前价格是否"接近"支撑线

#### 计算公式
**文件**: `src/services/supportLineService.ts` 第224行

```typescript
// 计算距离百分比
const distance_percent = ((current_price - support_price) / support_price) * 100;

// 判断是否接近支撑线（±1%）
const is_near_support = Math.abs(distance_percent) <= 1;
```

#### 距离状态说明

| 距离百分比 | 状态 | 含义 | 示例 |
|-----------|------|------|------|
| **-1.0% ~ +1.0%** | ✅ **低吸机会** | 价格在支撑线附近，可以考虑买入 | 支撑线400，当前价396-404 |
| **+1% ~ +5%** | ⚠️ **高于支撑线** | 价格偏高，等待回调 | 支撑线400，当前价410 |
| **> +5%** | ❌ **正常** | 价格远离支撑线，不是低吸时机 | 支撑线400，当前价450 |
| **< -1%** | ⚠️ **跌破支撑线** | 价格已跌破，可能需要调整支撑线 | 支撑线400，当前价390 |

#### 前端显示逻辑
**文件**: `public/static/pattern.js` 第586-612行

```javascript
// 你截图中的"距离"列显示
const distance = ((currentPrice - supportPrice) / supportPrice * 100).toFixed(2);

// 状态判断
const isNear = Math.abs(distance) <= 1;

// 显示颜色
const distanceColor = distance > 1 ? 'text-blue-600' :  // 高于支撑线（蓝色）
                      distance < -1 ? 'text-red-600' :   // 跌破支撑线（红色）
                      'text-green-600';                   // 接近支撑线（绿色）

// 状态标签
if (isNear) {
  // 显示"低吸机会"标签（绿色）
  '<span class="px-2 py-1 bg-green-500 text-white rounded text-xs font-bold">低吸机会</span>'
}
```

---

### 4️⃣ 市场策略判断

**目的**: 避免在市场趋势极差时抄底

#### 市场策略来源
**数据来源**: `trading_rules` 表的 `notes` 字段

#### 三种市场状态

| 市场策略 | 含义 | 是否允许低吸 | 说明 |
|---------|------|-------------|------|
| **单边主升** | 市场强势上涨 | ✅ 允许 | 回调是好的买入机会 |
| **双边震荡** | 市场上下波动 | ✅ 允许 | 可以低买高卖 |
| **单边主跌** | 市场持续下跌 | ❌ **禁止** | 抄底容易被套，风险极高 |

#### 判断逻辑
**文件**: `src/services/supportLineService.ts` 第229-236行

```typescript
// 从notes中提取市场策略
const market_notes = row.market_notes || '';
const market_strategy = market_notes.includes('单边主跌') ? '单边主跌' : 
                        market_notes.includes('单边主升') ? '单边主升' : 
                        '双边震荡';

// 只有在非单边下跌市场中才可以做多
const can_long = market_strategy !== '单边主跌' && row.long_allowed === 1;
```

**关键**: 
- 如果 `market_strategy === '单边主跌'`，则 `can_long = false`
- 即使其他条件都满足，也**不会显示为低吸机会**

---

### 5️⃣ 交易规则判断

**目的**: 遵守人工设置的交易限制

#### 交易规则表
**表**: `trading_rules`
**关键字段**:
- `trading_allowed`: 是否允许交易（0=禁止，1=允许）
- `long_allowed`: 是否允许做多（0=禁止，1=允许）
- `short_allowed`: 是否允许做空（0=禁止，1=允许）

#### 判断逻辑
**文件**: `src/services/supportLineService.ts` 第191-194行

```sql
WHERE sl.date = ?
  AND cp.level <= 2
  AND tr.trading_allowed = 1  -- 必须允许交易
  AND tr.long_allowed = 1     -- 必须允许做多
```

**只有同时满足**：
- `trading_allowed = 1`
- `long_allowed = 1`

才会被列入低吸机会候选名单。

---

## 🔄 完整判断流程图

```
┌─────────────────────┐
│ 所有币种（29个）    │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 过滤：等级1-2       │  ← 条件1：level <= 2
│ ❌ 去除等级3-6      │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 过滤：已设置支撑线  │  ← 条件2：support_price > 0
│ ❌ 去除未设置的     │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 过滤：允许交易+做多 │  ← 条件5：trading_allowed=1 && long_allowed=1
│ ❌ 去除禁止交易的   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 计算：价格距离      │  ← 条件3：Math.abs(distance) <= 1%
│ 判断：是否在±1%内  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ 判断：市场策略      │  ← 条件4：market_strategy ≠ '单边主跌'
│ ❌ 去除单边主跌市场 │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│ ✅ 低吸机会列表     │
│ can_long = true     │
└─────────────────────┘
```

---

## 💡 你截图中的数据分析

### TAO (等级1)
```
当前价格: 430.6300
支撑线:   404
距离:     +6.59%
状态:     正常（未显示"低吸机会"）
```

**分析**：
- ✅ 等级1（满足条件1）
- ✅ 已设置支撑线 = 404（满足条件2）
- ❌ 距离 = +6.59%，**超过±1%范围**（不满足条件3）
- 结论：**不是低吸机会**，因为价格还没回调到支撑线附近

**什么时候会显示"低吸机会"？**
- 当价格跌到 **400 - 408** 之间（±1%）

### BCH (等级2)
```
当前价格: 562.1400
支撑线:   553
距离:     +1.65%
状态:     正常（未显示"低吸机会"）
```

**分析**：
- ✅ 等级2（满足条件1）
- ✅ 已设置支撑线 = 553（满足条件2）
- ❌ 距离 = +1.65%，**超过1%**（不满足条件3）
- 结论：**不是低吸机会**，价格略高于支撑线

**什么时候会显示"低吸机会"？**
- 当价格跌到 **547.5 - 558.5** 之间（±1%）

### BNB (等级2)
```
当前价格: 1105.8800
支撑线:   1126
距离:     -1.79%
状态:     正常（未显示"低吸机会"）
```

**分析**：
- ✅ 等级2（满足条件1）
- ✅ 已设置支撑线 = 1126（满足条件2）
- ❌ 距离 = -1.79%，**超过-1%**（不满足条件3）
- 结论：**不是低吸机会**，价格已跌破支撑线

**问题**：
- 当前价格1105已经**低于支撑线1126**
- 说明支撑线设置可能过高
- 建议：调整支撑线到1100或更低

---

## 🎯 实战使用建议

### 1. 如何设置合理的支撑线？

#### 方法A: 基于历史回调
```
1. 查看币种K线图
2. 找到近期的重要支撑位（前期低点）
3. 设置支撑线在该位置或略低
```

#### 方法B: 基于当前价格下移
```
当前价格 × (1 - 回调预期) = 支撑线
例如：
- TAO当前430.63
- 预期回调5%
- 支撑线 = 430.63 × 0.95 = 409
```

#### 方法C: 基于技术指标
```
- 20日均线位置
- 斐波那契38.2%回调位
- 布林带下轨
- 整数关口（如400、500、1000）
```

### 2. 什么时候设置支撑线？

**推荐时机**：
- ✅ 币种刚完成一波上涨，预期会回调
- ✅ 价格接近前期重要支撑位
- ✅ 市场处于震荡或偏多状态
- ❌ 避免在单边主跌时设置（反弹概率低）

### 3. 看到"低吸机会"后怎么办？

**不是买入信号，而是提示！**

#### 再次确认：
1. **查看K线图**：是否确实到了支撑位？
2. **查看市场策略**：是否处于单边主跌？
3. **查看24h涨跌幅**：是否跌幅过大（>-10%）？
4. **查看成交量**：是否有放量迹象？

#### 决策参考：
- **符合3+确认条件** → 可以考虑小仓位试探
- **只满足基本条件** → 继续观察，不急于入场
- **市场极端下跌** → 即使显示机会也要谨慎

---

## 📊 API接口说明

### 获取支撑线列表
```http
GET /api/support-lines
```

**返回**：
```json
{
  "success": true,
  "count": 3,
  "lines": [
    {
      "symbol": "TAO",
      "support_price": 404,
      "date": "2025-10-29",
      "notes": "前期低点"
    }
  ]
}
```

### 检查低吸机会
```http
GET /api/support-lines/opportunities
```

**返回**：
```json
{
  "success": true,
  "total_opportunities": 3,
  "near_support_count": 1,
  "opportunities": [
    {
      "symbol": "TAO",
      "current_price": 400.5,
      "support_price": 404,
      "distance_percent": -0.87,
      "is_near_support": true,
      "coin_level": 1,
      "market_strategy": "双边震荡",
      "can_long": true
    }
  ]
}
```

### 设置/更新支撑线
```http
POST /api/support-lines
Content-Type: application/json

{
  "symbol": "TAO",
  "support_price": 404,
  "notes": "前期低点"
}
```

### 删除支撑线
```http
DELETE /api/support-lines/{symbol}
```

---

## 🔧 系统维护

### 每日自动清零
**时间**: 每天北京时间 0:00
**动作**: 自动删除前一天的所有支撑线数据
**原因**: 支撑线需要根据每天的市场情况重新设置

### 历史数据清理
**时间**: 定期执行
**动作**: 删除7天前的支撑线历史数据
**原因**: 减少数据库存储压力

---

## ⚠️ 重要风险提示

### 1. 这不是买入信号
显示"低吸机会"只是**提示价格接近预设位置**，不是确定的买入建议。

### 2. 支撑可能被跌破
支撑线不是绝对的，市场极端情况下会直接跌破，需要设置止损。

### 3. 单边主跌禁止抄底
系统会自动屏蔽单边主跌市场的低吸机会，但这是基于当前数据判断，市场可能快速变化。

### 4. 币种等级会变化
等级1-2的币种会随着价格变化而调整等级，可能失去设置支撑线的资格。

### 5. 需要人工复核
所有机会都应该人工确认后再操作，不要盲目依赖系统提示。

---

## 📝 总结

### 低吸逻辑核心三要素
1. **质量过滤**：只看等级1-2币种（远离历史低点）
2. **价格触发**：当前价格进入支撑线±1%范围
3. **风险控制**：市场策略允许 + 交易规则允许

### 适用场景
✅ 震荡市场的低买高卖  
✅ 优质币种回调时的加仓  
✅ 技术分析的辅助工具  

### 不适用场景
❌ 单边主跌市场的抄底  
❌ 低质量币种的博反弹  
❌ 作为唯一的买入依据  

---

**文档版本**: v1.0  
**最后更新**: 2025-10-29  
**维护人**: AI Assistant
