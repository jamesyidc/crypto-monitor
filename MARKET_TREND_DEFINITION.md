# 📊 今日市场状态 - 5种趋势定义与判断逻辑

> **文档目的**: 明确定义"今日市场状态"的5种趋势类型及其判断规则  
> **创建时间**: 2025-10-29  
> **适用范围**: Crypto Monitor 系统 - 市场趋势分析模块

---

## 📍 功能位置

今日市场状态显示在首页的"市场趋势分析"卡片中：

```
┌────────────────────────────────┐
│      今日市场状态              │
│                                │
│      ┌──────────────┐          │
│      │  单边主升    │          │
│      └──────────────┘          │
│          ★★★                   │
└────────────────────────────────┘
```

---

## 🎯 5种市场状态定义

### 类型定义

```typescript
// src/types.ts
export type MarketTrend = 
  | '单边主升'    // 强势上涨市场
  | '震荡偏多'    // 偏多震荡市场
  | '无序震荡'    // 无明确方向震荡
  | '震荡偏空'    // 偏空震荡市场
  | '单边主跌';   // 强势下跌市场
```

---

## 1️⃣ 单边主升

### 📖 定义
**市场处于强势单边上涨趋势，多头主导**

### 🔢 判断条件
```javascript
前置条件: 今日急涨次数 >= 10

主要判断:
- 新高新低差值 = 今日创新高次数 - 今日创新低次数
- 新高新低差值 >= 3

结论: 单边主升
```

### 📊 数据来源
- **今日急涨次数** (`surge_count`): 当日累计急涨次数（涨幅≥+1%）
- **今日创新高次数** (`todayNewHighCount`): 当日币种创历史新高的累计次数
- **今日创新低次数** (`todayNewLowCount`): 当日币种创历史新低的累计次数

### 🎨 显示特征
- **颜色**: 深绿色 (`green`)
- **星级**: ★ / ★★ / ★★★ （实心黑星）
- **星级类型**: 急涨

### 📝 业务含义
- 市场强势上涨，多头力量压倒性优势
- 大量币种创新高（至少比创新低多3个以上）
- 适合做多，但需警惕短期过热
- 通常出现在牛市加速期或重大利好后

### 💻 代码实现
```typescript
// src/services/analysisService.ts (第300-313行)
if (surges >= 10) {
  const highLowDiff = newHighs - newLows;
  
  if (highLowDiff >= 3) {
    trend = '单边主升';
  }
}
```

---

## 2️⃣ 震荡偏多

### 📖 定义
**市场震荡为主，但多头略占优势**

### 🔢 判断条件
```javascript
前置条件: 今日急涨次数 >= 10

主要判断:
- 新高新低差值 = 今日创新高次数 - 今日创新低次数
- 1 <= 新高新低差值 < 3

结论: 震荡偏多
```

### 🎨 显示特征
- **颜色**: 浅绿色 (`green-light`)
- **星级**: ★ / ★★ / ★★★ （实心黑星）
- **星级类型**: 急涨

### 📝 业务含义
- 市场震荡，但多头稍占上风
- 创新高币种略多于创新低币种（1-2个差距）
- 涨跌交替，方向不明确
- 适合短线操作，注意止损

### 💻 代码实现
```typescript
// src/services/analysisService.ts (第311-312行)
if (surges >= 10) {
  const highLowDiff = newHighs - newLows;
  
  if (highLowDiff >= 1 && highLowDiff < 3) {
    trend = '震荡偏多';
  }
}
```

---

## 3️⃣ 无序震荡 ⭐ 默认状态

### 📖 定义
**市场方向不明，多空力量均衡**

### 🔢 判断条件
```javascript
默认状态（两种情况）:

情况1: 今日急涨次数 < 10 且 今日急跌次数 < 10
→ 市场活跃度不足，无法判断趋势

情况2: 急涨 >= 10 时，新高新低差值 < 1
    或 急跌 >= 10 时，新低新高差值 < 1
→ 虽然市场活跃，但多空均衡

结论: 无序震荡
```

### 🎨 显示特征
- **颜色**: 灰色 (`gray`)
- **星级**: 无星级
- **星级类型**: null

### 📝 业务含义
- 市场方向不明，观望为主
- 两种可能：
  1. 市场清淡，缺乏明确趋势
  2. 多空僵持，涨跌势均力敌
- 不适合大仓位操作
- 等待明确信号再入场

### 💻 代码实现
```typescript
// src/services/analysisService.ts (第293行，第350行)
let trend: MarketTrend = '无序震荡';  // 默认值

// 如果急涨和急跌都 < 10，则不计算比值和星级（保持默认值）
if (surges < 10 && crashes < 10) {
  // 保持 trend = '无序震荡'
}
```

---

## 4️⃣ 震荡偏空

### 📖 定义
**市场震荡为主，但空头略占优势**

### 🔢 判断条件
```javascript
前置条件: 今日急跌次数 >= 10

主要判断:
- 新低新高差值 = 今日创新低次数 - 今日创新高次数
- 1 <= 新低新高差值 < 3

结论: 震荡偏空
```

### 🎨 显示特征
- **颜色**: 浅红色 (`red-light`)
- **星级**: ☆ / ☆☆ / ☆☆☆ （空心黑星）
- **星级类型**: 急跌

### 📝 业务含义
- 市场震荡，但空头稍占上风
- 创新低币种略多于创新高币种（1-2个差距）
- 涨跌交替，但下跌压力略大
- 需要谨慎，控制仓位

### 💻 代码实现
```typescript
// src/services/analysisService.ts (第336-337行)
if (crashes >= 10) {
  const lowHighDiff = newLows - newHighs;
  
  if (lowHighDiff >= 1 && lowHighDiff < 3) {
    trend = '震荡偏空';
  }
}
```

---

## 5️⃣ 单边主跌

### 📖 定义
**市场处于强势单边下跌趋势，空头主导**

### 🔢 判断条件
```javascript
前置条件: 今日急跌次数 >= 10

主要判断:
- 新低新高差值 = 今日创新低次数 - 今日创新高次数
- 新低新高差值 >= 3

结论: 单边主跌
```

### 🎨 显示特征
- **颜色**: 深红色 (`red`)
- **星级**: ☆ / ☆☆ / ☆☆☆ （空心黑星）
- **星级类型**: 急跌

### 📝 业务含义
- 市场强势下跌，空头力量压倒性优势
- 大量币种创新低（至少比创新高多3个以上）
- 高风险环境，建议观望或轻仓
- 通常出现在熊市加速期或重大利空后

### 💻 代码实现
```typescript
// src/services/analysisService.ts (第334-335行)
if (crashes >= 10) {
  const lowHighDiff = newLows - newHighs;
  
  if (lowHighDiff >= 3) {
    trend = '单边主跌';
  }
}
```

---

## 🔄 判断流程图

```
开始
  ↓
判断：今日急涨次数 >= 10？
  ↓ 是
  计算: 新高新低差值 = 新高次数 - 新低次数
  ↓
  差值 >= 3？ → 是 → 【单边主升】★★★
  ↓ 否
  差值 >= 1？ → 是 → 【震荡偏多】★★
  ↓ 否
  → 【无序震荡】（无星级）
  
判断：今日急跌次数 >= 10？
  ↓ 是
  计算: 新低新高差值 = 新低次数 - 新高次数
  ↓
  差值 >= 3？ → 是 → 【单边主跌】☆☆☆
  ↓ 否
  差值 >= 1？ → 是 → 【震荡偏空】☆☆
  ↓ 否
  → 【无序震荡】（无星级）

如果急涨 < 10 且 急跌 < 10
  ↓
  → 【无序震荡】（无星级）
```

---

## 📊 判断条件对比表

| 市场状态 | 前置条件 | 核心判断 | 星级符号 | 颜色 |
|---------|---------|---------|---------|------|
| **单边主升** | 急涨≥10 | 新高-新低 ≥ 3 | ★★★ | 深绿 |
| **震荡偏多** | 急涨≥10 | 1 ≤ 新高-新低 < 3 | ★★ | 浅绿 |
| **无序震荡** | 急涨<10 且 急跌<10 | 默认状态 | 无 | 灰色 |
| **震荡偏空** | 急跌≥10 | 1 ≤ 新低-新高 < 3 | ☆☆ | 浅红 |
| **单边主跌** | 急跌≥10 | 新低-新高 ≥ 3 | ☆☆☆ | 深红 |

---

## ⭐ 星级评定系统

### 星级计算（仅在单边趋势时显示）

**急涨主导时（实心星★）**:
```javascript
比值 = (急涨次数 - 急跌次数) ÷ 急跌次数

比值 >= 1 且 < 2  → ★   一星
比值 >= 2 且 < 3  → ★★  二星
比值 >= 3         → ★★★ 三星
```

**急跌主导时（空心星☆）**:
```javascript
比值 = (急跌次数 - 急涨次数) ÷ 急涨次数

比值 >= 1 且 < 2  → ☆   一星
比值 >= 2 且 < 3  → ☆☆  二星
比值 >= 3         → ☆☆☆ 三星
```

### 星级含义

| 星级 | 含义 | 强度 |
|------|------|------|
| ★ / ☆ | 轻度趋势 | 比值1-2倍，趋势初现 |
| ★★ / ☆☆ | 中度趋势 | 比值2-3倍，趋势明确 |
| ★★★ / ☆☆☆ | 强势趋势 | 比值≥3倍，趋势强劲 |

---

## 💡 关键概念说明

### 1. 急涨/急跌 vs 创新高/新低

**急涨/急跌**（相对上一轮次）:
- **急涨**: 相对上一轮次涨幅 ≥ +1%
- **急跌**: 相对上一轮次跌幅 ≤ -1%
- **用途**: 判断是否达到前置条件（≥10次）

**创新高/新低**（历史极值）:
- **创新高**: 当前价格 > 历史最高价
- **创新低**: 当前价格 < 历史最低价
- **用途**: 计算差值判断市场趋势方向

### 2. 为什么需要10次门槛？

```
急涨/急跌 >= 10 的意义:

1. 确保市场活跃度足够
   - < 10次: 市场清淡，偶然波动
   - >= 10次: 市场活跃，趋势可信

2. 过滤噪音
   - 少量币种的波动不代表整体趋势
   - 需要至少1/3的币种（29只中的10只）有明确方向

3. 提高判断准确性
   - 避免过度敏感
   - 确保信号质量
```

### 3. 新高新低差值的作用

```
差值 = 新高次数 - 新低次数（或反过来）

差值含义:
- 差值 >= 3: 明显的单边趋势（强势）
- 差值 1-2: 轻度偏向某一方（震荡）
- 差值 < 1: 多空均衡（无序）

示例:
- 创新高10次，创新低0次 → 差值=10 → 单边主升
- 创新高12次，创新低10次 → 差值=2 → 震荡偏多
- 创新高8次，创新低7次 → 差值=1 → 震荡偏多
- 创新高5次，创新低5次 → 差值=0 → 无序震荡
```

---

## 📝 完整计算逻辑

### 代码实现（完整版）

```typescript
// src/services/analysisService.ts (第292-352行)
private calculateMarketTrend(
  surges: number,      // 今日急涨次数
  crashes: number,     // 今日急跌次数
  newHighs: number,    // 今日创新高次数
  newLows: number      // 今日创新低次数
) {
  let trend: MarketTrend = '无序震荡';  // 默认状态
  let strength = 0;
  let starRating = 0;
  let starType: StarType | null = null;

  const highLowDiff = newHighs - newLows;

  // 判断1: 急涨主导（急涨次数 >= 10）
  if (surges >= 10) {
    const diff = surges - crashes;
    strength = crashes > 0 ? diff / crashes : diff;

    // 根据新高新低差值判断趋势
    if (highLowDiff >= 3) {
      trend = '单边主升';
    } else if (highLowDiff >= 1) {
      trend = '震荡偏多';
    }
    // 如果 highLowDiff < 1，保持默认 '无序震荡'

    // 星级评定（实心黑星★）
    starType = '急涨';
    if (strength >= 1 && strength < 2) {
      starRating = 1;  // ★
    } else if (strength >= 2 && strength < 3) {
      starRating = 2;  // ★★
    } else if (strength >= 3) {
      starRating = 3;  // ★★★
    }
  } 
  // 判断2: 急跌主导（急跌次数 >= 10）
  else if (crashes >= 10) {
    const diff = crashes - surges;
    strength = surges > 0 ? diff / surges : diff;

    // 根据新低新高差值判断趋势
    const lowHighDiff = newLows - newHighs;
    if (lowHighDiff >= 3) {
      trend = '单边主跌';
    } else if (lowHighDiff >= 1) {
      trend = '震荡偏空';
    }
    // 如果 lowHighDiff < 1，保持默认 '无序震荡'

    // 星级评定（空心黑星☆）
    starType = '急跌';
    if (strength >= 1 && strength < 2) {
      starRating = 1;  // ☆
    } else if (strength >= 2 && strength < 3) {
      starRating = 2;  // ☆☆
    } else if (strength >= 3) {
      starRating = 3;  // ☆☆☆
    }
  }
  // 判断3: 市场清淡（急涨 < 10 且 急跌 < 10）
  // 保持默认值 trend = '无序震荡', starRating = 0, starType = null

  return { trend, strength, starRating, starType };
}
```

---

## 🎯 实际应用示例

### 示例1: 牛市行情
```
数据:
- 急涨次数: 15
- 急跌次数: 3
- 创新高次数: 8
- 创新低次数: 1

计算:
1. surges (15) >= 10 ✅ 进入急涨判断
2. highLowDiff = 8 - 1 = 7
3. 7 >= 3 ✅

结果: 单边主升 ★★★
比值: (15-3)/3 = 4 (>3, 三星)
```

### 示例2: 震荡市
```
数据:
- 急涨次数: 12
- 急跌次数: 10
- 创新高次数: 5
- 创新低次数: 3

计算:
1. surges (12) >= 10 ✅ 进入急涨判断
2. highLowDiff = 5 - 3 = 2
3. 1 <= 2 < 3 ✅

结果: 震荡偏多 ★
比值: (12-10)/10 = 0.2 (<1, 无星级实际上应该没有星)
```

### 示例3: 清淡市场
```
数据:
- 急涨次数: 5
- 急跌次数: 6
- 创新高次数: 2
- 创新低次数: 1

计算:
1. surges (5) < 10 ❌
2. crashes (6) < 10 ❌

结果: 无序震荡（默认状态）
星级: 无
```

---

## 📚 相关文件索引

### 后端文件
- `src/types.ts` (第120-128行) - 类型定义
- `src/services/analysisService.ts` (第292-352行) - 核心计算逻辑
- `src/services/analysisService.ts` (第256-287行) - 数据收集和调用

### 前端文件
- `public/static/app.js` - 市场趋势显示逻辑
  - `renderMarketTrend()` 函数
  - 趋势颜色映射
  - 星级显示

### 数据库表
- `daily_stats` - 每日统计表
  ```sql
  - date: 日期
  - symbol: 币种
  - total_surges: 当日急涨累计次数
  - total_crashes: 当日急跌累计次数
  - trend: 市场趋势（5种状态之一）
  - star_rating: 星级（0-3）
  - star_type: 星级类型（急涨/急跌）
  ```

---

## ⚠️ 重要注意事项

### 1. 数据重置时机
- 每日北京时间0点清零急涨急跌累计次数
- 创新高新低次数也会清零
- 市场状态重新开始计算

### 2. 趋势滞后性
- 市场趋势基于累计数据
- 需要一定的数据积累（至少10次急涨/跌）
- 早盘可能显示"无序震荡"直到数据足够

### 3. 星级独立性
- 星级仅在单边趋势时显示
- 震荡状态下星级意义不大
- 星级评定基于急涨急跌比值，与新高新低无关

### 4. 特殊情况
```
情况1: 急涨10次，但新高新低差值<1
→ 结果: 无序震荡（虽然有急涨，但创新高不多）

情况2: 急涨15次，急跌14次，新高新低差值5
→ 结果: 单边主升 ★（虽然差值大，但比值小）

情况3: 急涨和急跌都>=10
→ 结果: 只会判断一个（先判断急涨）
```

---

## ✅ 总结

### 5种状态优先级

```
1. 急涨 >= 10 且 新高新低差 >= 3  → 单边主升
2. 急涨 >= 10 且 新高新低差 >= 1  → 震荡偏多
3. 急跌 >= 10 且 新低新高差 >= 3  → 单边主跌
4. 急跌 >= 10 且 新低新高差 >= 1  → 震荡偏空
5. 其他所有情况                  → 无序震荡
```

### 核心判断要素

| 要素 | 作用 | 阈值 |
|------|------|------|
| **急涨次数** | 判断是否进入多头分析 | ≥ 10 |
| **急跌次数** | 判断是否进入空头分析 | ≥ 10 |
| **新高新低差值** | 确定趋势强度 | ≥3单边, 1-2震荡 |
| **急涨急跌比值** | 计算星级 | ≥1一星, ≥2二星, ≥3三星 |

### 实战意义

- **单边主升**: 强势做多，注意止盈
- **震荡偏多**: 逢低做多，控制仓位
- **无序震荡**: 观望为主，等待信号
- **震荡偏空**: 谨慎操作，轻仓试空
- **单边主跌**: 回避风险，等待底部

---

**文档版本**: v1.0  
**最后更新**: 2025-10-29  
**维护人**: AI Assistant
